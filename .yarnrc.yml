nodeLinker: node-modules

# Allow installing the latest published package versions. Yarn 4.15 introduced a
# supply-chain "age gate" that quarantines very recently published releases by
# default; setting it to 0 lets Renovate-managed latest versions install in CI.
npmMinimalAgeGate: 0
