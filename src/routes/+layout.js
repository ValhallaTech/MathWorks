// Prerender the entire app to static HTML. The calculator is fully
// client-side (no server load functions), so it can be served as static
// files by Netlify via @sveltejs/adapter-netlify.
export const prerender = true;
